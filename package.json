{
  "name": "dayflow",
  "version": "0.1.0",
  "private": true,
  "description": "DayFlow - HR management system (hackathon project)",
  "scripts": {
    "setup": "npm install --prefix frontend && npm install --prefix backend"
  }
}
